# AnarchyClient

Fabric `26.1.2` utility mod for 6b6t.

## Development

```bash
./gradlew build
./gradlew runClient
```

The client menu opens with `Right Shift`. Rivet is vendored under `src/main/java/net/lenni0451/rivet` and its upstream license is preserved in `third_party/rivet`.
